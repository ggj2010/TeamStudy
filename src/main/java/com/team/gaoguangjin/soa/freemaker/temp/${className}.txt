package ${package};
import lombok.Getter;
import lombok.Setter;

<#list listColumn as c>
private ${c.type} ${c.name};
private ${c.type} get${c.name?cap_first}() {  
	return ${c.name};

 private void set${c.name?cap_first}(${c.type} ${c.name}) {  
 	this.${c.name}=${c.name};
 }
}
</#list>
